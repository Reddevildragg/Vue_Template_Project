# Browser Extension

This template sets up the project to be a browser extension.

## Development

- `npm run dev`: Build the extension in watch mode.
- `npm run build`: Build the extension for production.

To load the extension in your browser, load the built directory (usually `dist` or the root folder depending on your setup) as an unpacked extension.

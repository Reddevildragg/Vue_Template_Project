# Web Application

This is a standard web application setup.

## Development

- `npm run dev`: Start the development server.
- `npm run build`: Build the application for production.
- `npm run preview`: Preview the production build locally.

## Deployment

This project is configured to deploy as a web application. The `.github/workflows/deploy-webapp.yml` workflow (if present) handles the deployment.
